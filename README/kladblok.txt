//opruimenen van de editor leeg maken:
rm -rf storage && npm run dev

// git ignore leeg maken per file.
git rm --cached <file_or_directory_name>

//commits samenvoegen en sturen naar github
git reset --soft HEAD~1
// na fout terug naar head
git reset --soft ORIG_HEAD 
git push origin HEAD --force

// push en pull tegelijk en mijn verschil in de puch niet er na 
git config pull.rebase false
git pull && git push

//npm uit aan script en opschonen RAM
scripts/reset.sh
CLEAN_CODE_SERVER=1 RESET_SWAP=1 bash scripts/reset.sh

// herstart code-server
sudo systemctl restart code-server@$USER


